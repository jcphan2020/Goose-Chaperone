#Run this if download fails, hopefully should take care of 
#most dependency issues
#code runs on python3
#haven't tested, adding to as I go, hopefully works
#Add additional modules if needed

pip3 install setuptools numpy pyasn1-modules tensorflow opencv-python \
    keyboard imutils 
